name: nine_s
description: >
  The 9S Protocol - Five frozen operations. Everything is a Scroll.
  Inspired by Plan 9's "everything is a file" and SICP's data abstraction.
version: 0.1.0
repository: https://github.com/obiverse/nine_s_dart

environment:
  sdk: ^3.0.0

dependencies:
  crypto: ^3.0.3
  collection: ^1.18.0
  pointycastle: ^3.7.4

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
